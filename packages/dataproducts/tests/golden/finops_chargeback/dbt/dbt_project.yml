name: finops_chargeback
version: 1.1.0
config-version: 2
profile: finops_chargeback
model-paths:
- models
test-paths:
- tests
target-path: target
clean-targets:
- target
- dbt_packages
models:
  finops_chargeback:
    +materialized: table
    +on_schema_change: append_new_columns
    +database: OBSERVABILITY
    +schema: PUBLISHED
    +tags:
    - chargeback
    - confidential
vars:
  product_id: finops_chargeback
  product_version: 1.1.0
  freshness_target_minutes: 480
